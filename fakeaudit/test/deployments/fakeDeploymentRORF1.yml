apiVersion: apps/v1beta1
kind: Deployment
metadata:
    name: fakeDeploymentRORF1
    namespace: fakeDeploymentRORF
spec:
    template:
        metadata:
            labels:
                apps: fakeReadOnlyRootFilesystem
        spec:
            containers:
            - name: fakeContainerRORF

