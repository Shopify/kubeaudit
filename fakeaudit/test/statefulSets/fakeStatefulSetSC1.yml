apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
    name: fakeStatefulSetSC1
    namespace: fakeStatefulSetSC
spec:
    template:
        metadata:
            labels:
                apps: fakeSecurityContext
        spec:
            containers:
            - name: fakeContainerSC

