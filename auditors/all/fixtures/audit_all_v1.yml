apiVersion: apps/v1
kind: Deployment
metadata:
  name: allAudits 
  namespace: fakeDeploymentSC
spec:
  template:
    metadata:
      labels:
        apps: fakeSecurityContext
    spec:
      hostPID: true
      hostIPC: true
      hostNetwork: true
      containers:
      - name: fakeContainerSC

---

apiVersion: v1
kind: Namespace
metadata:
  name: "fakeDeploymentSC"
