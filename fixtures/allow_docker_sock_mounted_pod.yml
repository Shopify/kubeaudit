apiVersion: v1
kind: Pod
metadata:
    name: Pod
    labels:
      audit.kubernetes.io/pod/allow-mount-docker-sock: "some reason"
spec:
    containers:
    - name: container
      volumeMounts:
        - mountPath: /var/run/docker.sock
          name: docker-sock-volume
    - name: container2
      volumeMounts:
        - mountPath: /var/run/docker.sock
          name: docker-sock-volume
    - name: container3
    volumes:
      - name: docker-sock-volume
        hostPath:
          path: /var/run/docker.sock
